import { Link } from 'react-router-dom';

const Profile = () => {

    return (
        <div className={styles.profilePage}>
            <h1>profile page</h1>
        </div>
    )
};

export default Profile;