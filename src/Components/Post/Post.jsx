import { Link } from 'react-router-dom';
import styles from './Post.module.css'

const Post = () => {

    return (
        <div className={styles.postPage}>
            <h1>post page</h1>
        </div>
    )
};

export default Post;